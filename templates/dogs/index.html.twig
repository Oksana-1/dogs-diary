{% extends 'base.html.twig' %}
{% block body %}
    <section
        id="dog-detail-app"
        data-props="{{ {
            dogId: dogId,
            mediaUrl: asset('images/rusty.mp4')
        }|json_encode|e('html_attr') }}"
    >
        <span class="vue-loading">Loading dog details…</span>
    </section>
{% endblock %}
