{% extends 'base.html.twig' %}
{% block body %}
    <section
        id="dog-detail-app"
        data-props="{{ {
            dogId: dogId
        }|json_encode|e('html_attr') }}"
    >
        <span class="vue-loading">Loading dog details…</span>
    </section>
{% endblock %}
