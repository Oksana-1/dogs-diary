{% extends 'base.html.twig' %}
{% block body %}
    <div>General dogs count = {{ dogs | length }}</div>
    <a href="{{ path('app_dog_index', { id: myDog.id} ) }}">{{ myDog.name }}</a>
{% endblock %}
