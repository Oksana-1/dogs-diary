# Symfony Runtime requires a base dotenv file. These fail-closed defaults do
# not contain credentials; Railway environment variables must override them.
APP_ENV=prod
APP_DEBUG=0
APP_SECRET=
DATABASE_URL=
MAILER_DSN=
MAILER_FROM_ADDRESS=
MAILER_FROM_NAME="Dogs Diary"
APP_BASE_URL=
